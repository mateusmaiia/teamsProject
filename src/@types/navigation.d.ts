export declare global {
  namespace ReactNavigation {
    interface RootParamList {
      //coloca as rotas e a tipagem é o parametro que a rota/componente tem
      grous: undefined;
      newGrous: undefined;
      player: {
        group: string;
      };
    }
  }
}
